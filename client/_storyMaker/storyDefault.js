storyDefault_cue = function( _name ) {

	var _cue = [];
	
	if ( _name == "default") {

		_cue  = [

					'story.da.add()',
					'story.da.show()',
					'story.da.fadeIn()'

				];	
	}

	return _cue;			

}